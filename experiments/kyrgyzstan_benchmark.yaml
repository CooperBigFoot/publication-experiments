# Kyrgyzstan Benchmark Experiment
# Training 4 architectures (EA-LSTM, TiDE, TSMixer, TFT) on 49 Kyrgyzstan basins
# All models use ~250k parameters, forecast mode, and basin_nse loss

models:
  ealstm: configs/models/ealstm_kyrgyzstan_benchmark.yaml
  tide: configs/models/tide_kyrgyzstan_benchmark.yaml
  tsmixer: configs/models/tsmixer_kyrgyzstan_benchmark.yaml
  tft: configs/models/tft_kyrgyzstan_benchmark.yaml

trainer:
  mode: evaluation # 60/20/20 train/val/test split with early stopping
  max_epochs: 150
  check_val_every_n_epoch: 1
  gradient_clip_val: 1.0
